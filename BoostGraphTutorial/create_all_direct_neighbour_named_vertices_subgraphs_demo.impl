#include <cassert>
#include "create_all_direct_neighbour_named_vertices_subgraphs.h"
#include "create_named_vertices_path_graph.h"

void create_all_direct_neighbour_named_vertices_subgraphs_demo() noexcept
{
  const auto v
    = create_all_direct_neighbour_named_vertices_subgraphs(
      create_named_vertices_path_graph( {"A","B","C"} )
    );
  assert(v.size() == 3);
}
