#include <string>
#include <boost/graph/properties.hpp>

template <typename graph>
std::string get_graph_name(
  const graph& g
) noexcept
{
  return
    get_property( //_not_ boost::get_property
      g, boost::graph_name
    );
}
